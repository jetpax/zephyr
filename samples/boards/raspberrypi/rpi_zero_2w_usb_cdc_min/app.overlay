/*
 * Phase 2.1 bring-up: enable the DWC2 USB controller as a device-mode
 * peripheral and declare a single CDC ACM UART child instance on it.
 *
 * The DWC2 node lives in bcm2710.dtsi (status="disabled" there); this
 * overlay flips it on for this sample only -- the board defconfig is
 * untouched, so the parallel wifi/SDIO work on the rpi-zero-2w-port
 * branch is not affected.
 *
 * Phase 2.4 (mailbox / VC power-domain): also enables &mailbox and
 * &firmware. The DWC2 vendor pre_enable hook issues SET_POWER_STATE
 * (USB_HCD, on) over the firmware property interface during USB init
 * to turn on the analog PHY power island that VC start.elf leaves
 * off. Without these two nodes enabled the USB controller is
 * digitally accessible but D+/D- aren't driven.
 */

&mailbox {
	status = "okay";
};

&firmware {
	status = "okay";
};

&usb {
	status = "okay";

	cdc_acm_uart0: cdc_acm_uart0 {
		compatible = "zephyr,cdc-acm-uart";
		label = "Zephyr USB CDC-ACM";
	};
};
